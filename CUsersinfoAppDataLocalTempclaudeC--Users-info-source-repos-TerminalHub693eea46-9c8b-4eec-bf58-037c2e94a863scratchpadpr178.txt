author:	zio3
association:	owner
edited:	false
status:	none
--
### Code review

No issues found. Checked for bugs and CLAUDE.md compliance.

Verified across 5 review dimensions (CLAUDE.md compliance, bug scan, git history, prior PR comments, in-code comment guidance):

- `RotateSessionProof` is called on all 3 ConPTY launch paths (lazy init / recreate / restart), inside the existing per-session init locks and double-check guards; no path passes a stale or missing proof to env/`-c` injection
- `ResolveBySessionProof` rejects null/empty proof before matching and excludes sessions with no proof set, so empty-vs-empty false matches cannot occur
- The `set_card` Description contradiction flagged in #177 is fully resolved here — the old `target`/`sessionId` arguments and their fallback wording were removed outright, and tool descriptions / MCP instructions / docs now agree on the proof contract (including "do not echo proof into messages")
- The `-c shell_environment_policy.set.TERMINALHUB_SESSION_PROOF` injection mirrors the #177 SESSION_ID pattern (user-supplied override wins, placed before `resume --last`); variable name intentionally avoids KEY/SECRET/TOKEN for Codex's `ignore_default_excludes`

Minor note (scored 75, below the 80 reporting threshold): the class doc's 「サーバーは状態を持たず…」 line and the docs' design-principles table (「エンベロープ/自己識別なし」「サーバーは状態を持たない」) now read as contradicting the per-session `SessionProof` state this PR introduces. A one-line scope clarification (e.g. "認証用 proof 等の最小限の状態は除く") would keep the design notes honest.

https://github.com/zio3/TerminalHub/blob/e78c73009477715d32fcc0f8debeb897eff169f7/TerminalHub/Mcp/SessionMessagingTools.cs#L15-L21

🤖 Generated with [Claude Code](https://claude.ai/code)

<sub>- If this code review was useful, please react with 👍. Otherwise, react with 👎.</sub>
--
