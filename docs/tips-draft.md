# TerminalHub Tips 候補リスト（ドラフト）

Discord などでの「Tips 投稿」用ネタ候補。週1〜隔週ペースでの発信を想定。
精査して採用/不採用、追記してください。

---

## 🟦 セッション基本

1. **同じセッションを別ブラウザ/タブで開ける** — Circuit 毎管理なので、PC とサブモニタで同じターミナルを表示できる
2. **セッションは「開いたとき」に起動する** — 遅延初期化なので、10 個並べてもバックグラウンドは静か
3. **ターミナル内の URL はクリックできる** — WebLinksAddon。`http://localhost:5173` みたいなのは即ブラウザへ
4. **入力履歴は永続化される** — 上下キーで過去入力。アプリ再起動後も残る
5. **タイトルバーに処理状態が出る** — 経過時間・トークン数がリアルタイム

## 🟩 Claude Code / Gemini / Codex 連携

6. **CLI の出力を解析して「処理中/完了」を自動判定** — Claude Code・Gemini・Codex 各対応
7. **5 秒間出力がないとハング判定** — タイマーで自動検出
8. **処理完了でベル通知** — 非アクティブセッションだけ鳴る（作業中のは黙る）
9. **トークン使用量が見える** — Claude/Gemini の使用量をセッション一覧でひと目
10. **Codex CLI も普通に使える** — Claude/Gemini と同じ枠で

## 🟧 Git Worktree

11. **セッションから Worktree を作れる** — 親ディレクトリの **兄弟** として作成（中ではなく横） → ✅記事化済み: https://gilded-pruner-343.notion.site/Git-Worktree-39669d5bd86b81878526d087d707bc46
12. **Worktree ごとに別 Claude Code を走らせる運用** — 同じリポジトリの別ブランチを並列で触れる → ✅記事化済み: https://gilded-pruner-343.notion.site/Git-Worktree-39669d5bd86b81878526d087d707bc46

## 🟨 タスクランナー / npm

13. **package.json を自動検出して npm scripts がタスクになる** — `npm run dev` をボタン化
14. **タスクは独立セッション** — 普通のターミナルと別管理。ログを残したまま別作業へ
15. **タスク選択状態は LocalStorage に保存** — 再起動後も同じタスク画面に戻る

## 🟥 通知 / Webhook / Hook

16. **Webhook 通知（処理開始・完了）を Slack/Discord に飛ばせる** — eventType=start/complete、JSON ペイロード
17. **閾値秒数を超えた処理だけ通知** — 短い処理で通知爆撃にならない
18. **Hook API (`/api/hook/claude/{sessionId}`)** — Claude Code 側 hook と直結。SessionStart/Stop/Notification を受信
19. **Webhook ペイロードに folderPath が入る** — どの作業ディレクトリの完了かが分かる

## 🟪 MQTT 遠隔起動（隠れ目玉）

20. **VPS 経由で外出先から PC のターミナルを起動できる** — MQTT 経由のリモートトリガ
21. **暗号化は RSA-OAEP + AES-256-GCM** — nonce リプレイ防止つき。本気の作り
22. **パスワード設定で他人からの起動はブロック** — SHA256 ハッシュ照合

## 🟫 起動モード / インストール

23. **App Mode（Chrome `--app`）でデスクトップアプリ風に起動** — `TerminalHub-App.bat`。タブバーなしで集中できる
24. **通常ブラウザモードもある** — `TerminalHub.bat`。既定ブラウザで開く
25. **インストーラー版は .NET ランタイム不要** — 自己完結 publish

## 🟦 トラブル時/小ワザ

26. **ログは `%LOCALAPPDATA%\TerminalHub\logs`** — エラー追跡はここ
27. **エラーレポート Webhook** — 例外発生時に自分宛 Discord に飛ばせる
28. **フォルダ選択ダイアログは最前面に出る** — v1.0.58 で修正済（App Mode で裏に隠れる問題）
29. **長い文字列を貼っても切れない** — ConPTY 書き込みを 265 文字チャンクで明示フラッシュ
30. **複数 TerminalHub プロセスを起動可能** — 開発インスタンスと通常利用を併存できる

---

## 🆕 直近の追加分（2026-06-14 以降 / v1.0.60〜1.0.63）

### 表示・レンダリング

31. **表示が二重になる/崩れる問題を根治** — 自作 VT エミュレータを全面採用（PR #91）。セッション切替時の再描画も Windows Terminal 準拠の挙動に（PR #95）
32. **xterm.js 6 + WebGL レンダラーに移行** — 描画が速く滑らかに（PR #71）
33. **ターミナルフォントを Cascadia Mono 化＋囲み数字（①②③）を可読化** — CJK フォールバック改善を含む（PR #84。記事化前に効果の実態を要裏取り）
34. **ターミナル右上に再描画ボタン** — 表示が乱れたらワンクリックで復旧（PR #65）

### モバイル対応

35. **スマホから普通に使える** — サイドバーはオーバーレイ表示（従来のモバイル対応）。タッチスクロールが効かない退行は PR #93 で修正
36. **カーソルパッド（D-pad）タブ** — 矢印キー・Enter・Esc をスマホから操作。＋メニューから追加（PR #92/#94）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39469d5bd86b81979272f16e51e1fc26
37. **入力待ちでカーソルパッドへ自動切替** — CLI が許可待ち/選択待ちになると自動でパッドが出る。スマホからの応答がスムーズ（PR #99・試験機能）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39469d5bd86b81979272f16e51e1fc26
38. **表示設定はデバイス別** — フォントサイズ・サイドバー幅などが PC とスマホで別々に保存される（PR #96）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39469d5bd86b81979272f16e51e1fc26

### Claude Code / Codex 連携の深化

39. **Claude Code hook でサブエージェント/compact/許可待ちを追跡** — 「今なにしてるか」がセッション一覧で見える（PR #73）
40. **通知アイコンが状態で変わる** — 停止=ベル / 許可待ち=八角形＋？（`bi-question-octagon`）/ 選択待ち=番号リスト（`bi-list-ol`）。何を求められているか一目で分かる（PR #77。アイコンは後続PRで現行の形に更新済み）
41. **Codex CLI も lifecycle hook 対応** — `TerminalHub.exe --notify` ブリッジ経由で Claude Code と同等の状態追跡・Webhook 通知（PR #82）
42. **Webhook は本来の hook イベント名で送信** — `UserPromptSubmit`/`Stop`/`SubagentStart` 等をそのまま転送。LED 連携などの受信側で自由に解釈できる

### Git / セッション運用

43. **Git バッジをクリックで変更ファイル一覧** — 赤丸（未コミット変更）の中身をダイアログで確認。LLM に聞かなくていい（PR #97）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39569d5bd86b8148a58af2d0653886fc
44. **Worktree は「ブランチを後で選ぶ」もできる** — 作成時にブランチ未定でも OK（PR #66）→ ✅記事化済み: https://gilded-pruner-343.notion.site/Git-Worktree-39669d5bd86b81878526d087d707bc46
    - **Git バッジの色で worktree を見分けられる**：通常リポジトリのブランチは緑、worktree は黄色のバッジになる。一覧を見るだけで「これは worktree のセッション」と一目で分かる → ✅記事化済み: 同上
45. **セッション専用カスタムコマンド** — セッションごとによく使うコマンドをボタン登録（PR #79）

### MCP（試験機能）

46. **MCPセッション間メッセージング** — TerminalHub が MCP サーバーになり、`send_to_session` でセッションから別セッションの CLI へメッセージ送信。Claude Code で作った仕様を Codex に渡す、みたいな連携が可能（PR #98）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39869d5bd86b819d9e4bdebd08ae8049
47. **MCP 設定の自動登録（既定OFF）** — 対応 CLI（Claude Code / Codex）へ terminalhub MCP を自動セットアップ（PR #98）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39869d5bd86b819d9e4bdebd08ae8049
48. **試験機能タブ新設** — 設定画面に試験的な機能の説明と ON/OFF を集約（PR #99）→ ✅記事化済み: https://gilded-pruner-343.notion.site/39869d5bd86b819d9e4bdebd08ae8049

### その他

49. **プレビュー版バッジ** — 先行ビルドには PR 番号/SHA/日時が表示され、正式版と区別できる（PR #89）
50. **Antigravity / Grok に承認スキップ・継続オプション** — Claude/Codex 以外の CLI も起動オプションが充実（PR #67）

### 2026-07 追加分（v1.0.64〜1.0.66）

51. **ターミナル内のファイル/フォルダ表記をクリックで開ける** — フォルダ=エクスプローラー / ファイル=既定アプリ。相対パス・フルパス・`file.cs:123` 形式に対応（PR #113 / v1.0.66）
52. **URL/パスの「タップでコピー」モード** — モバイル全画面などリンク先から戻れない環境向け。デバイス別設定（PR #103）
53. **Git 変更一覧から全ファイルパスをコピー** — 相対パス一覧をワンクリックでクリップボードへ。AI への指示に貼りやすい（PR #102）
54. **メモ機能** — セッションごとのメモ、自動スナップショット、削除時の退避（メモ管理ダイアログ）。MCP の `set_memo` で AI 自身に書かせる運用も
55. **コミットハッシュをクリックでコミット情報ダイアログ** — 件名・作者・日時・変更ファイル一覧（±行数）を表示、フルハッシュのコピーも。`a..b` 範囲表記にも対応（PR #114）

---

## 📚 記事化キュー（束ね方の整頓 / 2026-07-08）

1本の記事に関連ネタを束ねる方式（モバイル3選・Worktree回と同様）。上から順に出す想定。順番は入れ替え可。

### 公開済み（7本）
- 「セッション一覧の赤いアイコン3種類」（#40 の通知アイコン系を実質カバー）
- 「TerminalHub をスマホでそのまま使う（Tailscale）」
- 「モバイルで効くオプション3選」（#36/37/38）
- 「セッション一覧がミニGitダッシュボードになる」（#43）
- 「同じリポジトリで並行作業 — Git Worktree」（#11/12/44）
- 「セッション同士を AI に連携させる — TerminalHub MCP」（#46/47/48）: https://gilded-pruner-343.notion.site/39869d5bd86b819d9e4bdebd08ae8049

### 予定キュー
1. **専用コマンド** ← 次回。#45 セッション専用コマンド ＋ グローバルのカスタムコマンド（CLI別ボタン・テキスト挿入/直接送信モード）
2. **メモ機能** — #54。メモ管理・自動スナップショット・MCP `set_memo`（AI に作業状況を書かせる）
3. **ターミナル内リンク大全** — #3 URLクリック ＋ #51 パスリンク（v1.0.66 新機能）＋ #55 コミットハッシュ→情報ダイアログ ＋ #52 コピー動作モード ＋ #53 パス一覧コピー。「ターミナルの文字は掴める」で1本
4. **Webhook / Hook 連携** — #16/17/19 Webhook ＋ #18 Hook API ＋ #41/42 Codex hook・イベント名そのまま転送。LED 連携等の実例があると強い
5. **MQTT 遠隔起動（隠れ目玉）** — #20/21/22。外出先からPCのターミナル起動
6. **App Mode / 起動モード** — #23/24/25 ＋ #49 プレビュー版バッジ
7. **タスクランナー** — #13/14/15。npm scripts のボタン化
8. **地味に効く基本ネタ集** — #1 別ブラウザで同一セッション ＋ #2 遅延起動 ＋ #4 入力履歴永続化 ＋ #5 タイトルバー状態表示
9. **その他ストック** — #10/#50（CLI対応の広さ）、#29（長文貼り付け）、#30（多重起動）。単発ネタや他記事への足し合わせ用

### 記事化しない（当面）
- #31〜34（表示・レンダリング改善）: 内部品質の話でベネフィット記事にしにくい。リリースノートで十分
- #6/7/8/9（出力解析・通知の内部動作）: 「赤いアイコン3種類」記事で実質カバー済み
- #26〜28（トラブル時小ワザ）: 単発では弱い。サポート的な文脈が出てきたら

## 要確認の候補（裏取り未済み）

- セッションの並び順入れ替え（ドラッグできたか？）
- ターミナル設定（カラー/フォント）のカスタマイズ範囲
- DOS セッションの具体的なユースケース紹介
- App Mode 時の特殊な挙動（ショートカット等）

## 感触

- 30 本中、特に **#11/12 (Worktree)、#20–22 (MQTT)、#18 (Hook API)、#23 (App Mode)** あたりは「使ってるけど知らなかった」率が高そう
- 追加分 20 本（#31–50）を含めて計 50 本。週 1 なら約 1 年分、隔週なら 2 年分。ネタ的には十分回せる
- 直近分では **#37 (入力待ち自動パッド)、#40 (アイコンで状態区別)、#43 (Gitバッジクリック)、#46 (セッション間メッセージング)** が「知らないと存在に気づかない」系の筆頭
